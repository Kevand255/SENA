
// Crea una función llamada concatenarNombres que tome un nombre y un apellido como parámetros, 
// con un valor por defecto de una cadena vacía para ambos. La función debe devolver el nombre completo.

function concatenarNombres(nombre = "", apellido = "") {
    nombreCompleto = console.log("su nombre completo es: " + nombre + " " + apellido);
    return nombreCompleto;

}

concatenarNombres()
